services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DB_URI=mongodb://mongo:27017/HomExplorer
    depends_on:
      - mongo

  mongo:
    image: mongo
    ports:
      - "27017:27017"